{%- comment -%} Guardados: favoritos en localStorage, mismo patron que la bolsa. {%- endcomment -%}
<aside class="saved-drawer cart-drawer" data-saved-drawer aria-hidden="true" aria-label="Tus guardados">
  <div class="cart-drawer__head">
    <p class="eyebrow" style="margin:0">Guardados</p>
    <button class="cart-drawer__close" type="button" data-saved-close aria-label="Cerrar">✕</button>
  </div>
  <div class="cart-drawer__body" data-saved-body></div>
</aside>
